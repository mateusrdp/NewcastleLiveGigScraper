name: Run Update ICS on Main Push

on:
  push:
    branches:
      - main

jobs:
  run-update-ics:
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/update_ics.yml
